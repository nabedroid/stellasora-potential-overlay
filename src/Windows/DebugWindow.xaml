<Window x:Class="StellasoraPotentialOverlay.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StellasoraPotentialOverlay"
        mc:Ignorable="d"
        Title="OCR Debug Info" Height="600" Width="400"
        Topmost="True"
        ShowInTaskbar="False">
    <Grid>
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="認識されたテキスト一覧" Margin="10,10,10,5" FontWeight="Bold"/>
            <DataGrid x:Name="DebugResultsDataGrid" 
                      Margin="10" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="テキスト" Binding="{Binding RecognizedText}" Width="*"/>
                    <DataGridTextColumn Header="座標 (X,Y)" Binding="{Binding DisplayCoordinates}" Width="Auto"/>
                    <DataGridTextColumn Header="サイズ (W,H)" Binding="{Binding DisplaySize}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>
